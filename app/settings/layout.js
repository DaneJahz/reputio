export const metadata = {
  title: 'Settings — OwnerReply',
  description: 'Update your business name and AI response tone.',
};

export default function SettingsLayout({ children }) {
  return children;
}