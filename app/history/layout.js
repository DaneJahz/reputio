export const metadata = {
  title: 'Response History — OwnerReply',
  description: 'View all your posted Google review responses.',
};

export default function HistoryLayout({ children }) {
  return children;
}